 INTEGER*4 SEED(2),VAL
      CHARACTER*3 VALSTR
      REAL*4 MTH$RANDOM
      CALL SYS$GETTIM(SEED)
      VAL=100*MTH$RANDOM(SEED)
      WRITE(VALSTR,'(I3)') VAL
      CALL LIB$SET_SYMBOL('F_RANDOM',VALSTR)
      END
